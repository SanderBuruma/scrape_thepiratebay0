seriously this simply scrapes thepiratebay0 for new indie games that don't get mainstream attention but which (judging by the seeder count) are popular enough to be worth trying.

# Installation
1. Git clone
2. cd to folder
3. `python -m venv venv`
4. `source ./venv/bin/activate`
5. `pip install selenium webdriver_manager`